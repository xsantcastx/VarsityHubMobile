{
  "name": "varsityhub-expo-server",
  "version": "1.0.0",
  "scripts": {
    "start": "expo start --tunnel --port $PORT"
  },
  "dependencies": {
    "@expo/cli": "latest",
    "expo": "^54.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}