// GATED — restore when EVENTS is ready to test
export { default } from '@/components/ComingSoon';
// Root-level bridge so edit-event is reachable from root Stack navigation
// (deep links, notifications, etc.) — the real implementation lives in (tabs)/edit-event.tsx
// export { default } from './(tabs)/edit-event';
