const LEGAL_BASE_URL = 'https://varsityhub.app';

export const PUBLIC_TERMS_URL = `${LEGAL_BASE_URL}/terms`;
// Privacy policy is hosted on the canonical Lime Productions domain. The page
// MUST be published there (it was an empty placeholder) before shipping a build
// that links here — the in-app link and the App/Play listing are cross-checked.
export const PUBLIC_PRIVACY_POLICY_URL = 'https://limeprod.com/VarsityHubPrivacy';
export const PUBLIC_ACCOUNT_DELETION_URL = `${LEGAL_BASE_URL}/account-deletion`;
