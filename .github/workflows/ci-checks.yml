name: Deprecated CI Checks

on:
  # DEPRECATED: This workflow is kept for reference but disabled
  # Use .github/workflows/ci.yml instead which consolidates all CI checks
  workflow_dispatch:

jobs:
  deprecated:
    name: Deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Workflow retired
        run: |
          echo "This workflow is retired."
          echo "Use .github/workflows/ci.yml for CI and .github/workflows/verify-production-ready.yml for readiness audits."
