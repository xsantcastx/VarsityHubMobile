# September 5 remediation release verification

Latest follow-up: [data-export remediation](data-export-remediation.md), published from `cab5b133` to API `fc1e39a0`, OTA `7e09e4a5` (runtime 1.0.5) and the matching website. Code/delivery checks pass; private storage activation and physical iPhone archive handling remain open. [Matrix verification ledger](matrix-verification-ledger.md).

## Latest follow-up: session recovery

Current production product source is `8b8543e3`: API Railway `54143be0`, production OTA `1795b001` for iOS/Android runtime 1.0.5, and the matching website. The [session release record](session-restoration-remediation.md) includes 217 client suites / 1,534 assertions, six focused server suites / 38 assertions, five passing iPhone simulator scenarios, both public OTA delivery checks and eight public website checks. The owner's physical weekly logout remains unconfirmed. No new migration or native build was needed.

The earlier integration, schema cutover and publication evidence below is preserved for API `6bb32c40` / client `e6af820d`. Its other fixes remain included in the current product source. A separate [settings export gap](data-export-readiness-gap.md) is now explicitly open; a passing API readiness gate does not prove the export journey works.

## Release intent and source

Owner authorized fixes, verification and production API, OTA and website publication so they can perform device testing. This is a remediation release for testing; it does not certify A+ or every conceivable scenario. Audited base was `ec27781e`; integration began on `374a785b` on `fix/desktop-notes-map-collage-share`. No native dependency/config upgrade, EAS build or store submission is part of this release.

See [PDF note ledger](notes-remediation.md), [privacy/settings](privacy-settings-remediation.md), [roles/events](roles-events-remediation.md), [payments](payments-remediation.md), and [A+ gates](../../release/A_PLUS_READINESS_PLAN_2026-09-05.md). Earlier audit reports are before-fix evidence, not the final disposition.

## Completed integration evidence before publishing

- Client full suite: 215 suites, 1,511 assertions passed in one complete invocation after the Follow readiness, Followed Teams, production launcher, Stripe adapter, hydration/theme, explicit web-auth-entry and read-only empty-feed fixes. Jest is configured to force exit; passing counts do not prove no retained timers.
- Server suite: 319 suites and 2,982 assertions pass across the full sweep plus fresh-process reruns and final affected-file reruns; zero skipped. This is an aggregate, not one green full-suite process. The entitlement suite runs all six scenarios against the actual full app in a child Node process to avoid Jest's module-loader interference.
- Canonical `release:verify:local`: exit 0, including both TypeScript checks, access matrix, navigation (zero REVIEW), guardrails, regression suites, Expo doctor, coach approval wiring, local HTTP organization-manager access and email catalog/config checks.
- `release:verify:build`: exit 0 again after the final production client changes, with four nonblocking warnings. This checked build readiness; it did not build or submit binaries.
- PostgreSQL dump/restore and additive migration were rehearsed against a disposable local database. This is not a production backup restore drill.
- Production read-only preflight found two paid ads with five historical reservations, no future reservations, no active legacy checkout holds, no oversold future slots, and no ad repair required at the snapshot. No unresolved failed Prisma migration was present. The configured backup target was also reached read-only through its matching Railway private-domain/public-proxy mapping; its new table/column were absent before this release and must be verified after startup reconciliation. No backup URL or credential was changed.
- Dependency audit remains client 18 advisory nodes (8 high, 10 moderate) and server 3 moderate. No blind upgrade was applied. The available sanitize-html patch requires a newer Node runtime than this deployment; package upgrade plus supported runtime validation remains an explicit follow-up. Advisory counts are not demonstrated app exploits.

Detailed raw local logs live under `/tmp/varsityhub-remediation-2026-09-05/`; durable scenario descriptions and commands are in the linked handoffs. The audit fixtures use isolated loopback PostgreSQL and suppressed provider credentials. Production inspection is read-only, apart from the explicitly authorized deployment migration. No live test charge or test email was sent.

## Browser evidence

A fresh local Chromium run against the isolated API opened Feed, Highlights, Create, Create Post, Discover, Game Map, Profile, Settings and Followed Teams: all nine had zero browser page errors and zero app error boundaries. It recorded 79 API requests; this is a route smoke test, not a performance budget. Screenshots confirm [visible metallic media buttons](browser-evidence/ui-create-post.png), [real Discover icons](browser-evidence/ui-discover.png), and [Followed Teams in Dark appearance](browser-evidence/ui-settings-followed-dark.png). [Route result JSON](browser-evidence/route-smoke.json) records the nine destinations.

An actual settings interaction saved Private Profile, independently read `true` from `/me`, reloaded and observed the checked switch, selected Dark appearance, and opened Followed Teams with the dark text color (`rgb(148, 163, 184)`) and no error screen. Mock tests separately cover failed saves, unmount, stale refresh and account changes. An earlier browser attempt hit a local Metro heap limit and transient pre-fix bundles; the successful fresh run supersedes those smoke attempts, not their diagnostic history.

## Cutover and migration

Migration `20260905000000_ad_purchase_holds` adds `AdSlotHold` and nullable reservation purchase provenance. It does not guess ownership of old paid dates. New holds and existing purchased dates are separate so a failed Run Again purchase cannot erase paid inventory.

Old checkout writers do not count the new hold table. Railway's documented zero-overlap/zero-drain settings still permit a slight transition overlap; they are not an atomic writer boundary. For this release, stop the old API deployment and verify it removed before starting the new deployment. This causes a temporary API outage while the new image builds/starts. Confirm public API readiness, migration history and inventory postflight before the OTA and website publication. The startup placeholder now returns HTTP 503 with Retry-After, failed primary migrations abort startup, and migration/backup commands have bounded execution. Three executable shell/HTTP tests pass. The 600-second health-check window covers the default bounded startup work; real deployment readiness still requires post-start checks.

Rollback must retain the new inventory adapter after any new holds/purchases exist. Never blindly redeploy the old binary while new holds are active. The additive table/column can remain. If the new API never accepted traffic, verify no new holds before using the old binary as recovery. Avoid destructive schema rollback.

Railway references: [deployment lifecycle](https://docs.railway.com/deployments/reference#singleton-deploys) and [configuration](https://docs.railway.com/config-as-code/reference#deployment-teardown). The explicit stop boundary is required by this schema transition, not proof of general zero-downtime deployment.

Build-context exclusions now explicitly omit local dotenv files, private keys and agent state in both Railway uploads and Docker builds. Ignore-rule checks confirm these exclusions and preserve the Dockerfile's required source/manifests. No credential was rotated.

## Publication record

API source `6bb32c40740c492efa9c28ccbb7d7f23c3546cb8` is deployed as Railway `44e31b96-696a-4c2a-a150-e87e4e01f607` (SUCCESS). The previous deployment `083bbc8d-2655-496b-afed-558bb35f67bc` was stopped and verified REMOVED before the new upload. Migration `20260905000000_ad_purchase_holds` finished at **2026-09-05 15:13:57.53431 UTC**; the public monitor observed HTTP 200/status ok at **15:14:09.876756 UTC**. Monitoring began after the old deployment stopped, so those observations do not measure the entire outage.

Read-only postflight preserved all five historical paid reservations, found zero holds/pending purchases/expired holds/oversold future slots, and verified the new hold table and reservation purchase-reference column on the configured backup database. The runtime gate passed with 32/32 critical email templates configured; no email was sent. All three scoped posting grants were read back unchanged and expire September 12. [Sanitized production API evidence](production-api-verification.json) records the results and limits.

The first successful client publication used `04f861b826395dd3f7a49f113a6c195509eac456`: OTA group `a0d3bd8c-f0d2-453e-82ff-43ee146e5c88` on production/runtime 1.0.5 for iOS and Android, plus website deployment `varsityhub-8dg4wmrw7-emilmancero-devs-projects.vercel.app`. Publication and source-map upload succeeded, but subsequent production inspection found two additional release defects: the OTA did not inherit the build profile's Sentry DSN and production settings, and an empty bundled Stripe key left Android's SDK uninitialized despite the existing server-key fallback. Live static-web navigation also reproduced React hydration error 418. These publications are intermediate checkpoints, not the final accepted release.

The reusable production client launcher now validates production API/DSN settings and obtains a missing live publishable key from the existing public payment config before export. It imports only public defaults from the production build profile and forces production/no-dotenv flags. Its 22 real subprocess tests and a read-only effective-config probe pass. No account credential, EAS variable or Railway variable is rotated or changed. The runtime fallback now dispatches the public Stripe SDK before creating an intent (eight handler and six adapter checks pass). Shared hydration snapshots keep the first web viewport/theme render consistent with static HTML; explicit web sign-in/sign-up URLs are preserved. The final complete client suite passes, and a production-mode local static export passed 32 route/viewport/theme checks with zero page errors, hydration warnings or failed fonts/scripts/styles. API reads in that local rendering matrix use fixtures; it does not prove provider behavior. Shared Button/Input and sign-in also use the canonical application theme. Empty-feed browsing no longer POSTs to the sample-game creation endpoint; three new mounted guest/admin/retry regressions fail before and pass after this removal. A development-only React Navigation settings-header maxWidth attribute warning remains: the existing library corrects its frame after layout (verified at desktop/mobile widths, with working Appearance controls). It causes no page error or error boundary and is not suppressed. The final publications below supersede the intermediate artifacts.

## Final publication and owner testing

- **App source:** `e6af820df6498235758d49b924a41fcd8f53e747`, committed and pushed to the fork with normal commit/pre-push hooks. Both complete typechecks and pre-push guardrails passed. Server product source is identical to deployed `6bb32c40`; subsequent server-tree changes are test-only.
- **OTA:** [group 6f4b1579-d3f4-4a9a-9e8b-9594acc78e30](https://expo.dev/accounts/varsity-hub/projects/varsityhub/updates/6f4b1579-d3f4-4a9a-9e8b-9594acc78e30), published **2026-09-05 16:10:18.141 UTC**, production branch/channel, runtime **1.0.5**, iOS and Android. Both public update-delivery requests returned HTTP 200 with their exact new update IDs, production mode, Maps/Sentry present and a Stripe key matching the live API. The channel is active and maps wholly to production. Source-map upload succeeded. See [publication metadata](production-ota-verification.json) and [served-manifest evidence](production-ota-delivery.json).
- **Website:** [varsityhub.app](https://www.varsityhub.app) and its apex alias point to `varsityhub-lyt7ffbii-emilmancero-devs-projects.vercel.app`, deployment `dpl_4cdZGbFZvCrZ9DPnNxCPzV3mqF1W`, READY/production. The served JavaScript contains the expected live public Stripe key and production-profile Sentry DSN. Icon assets use the relocated vendor path and return HTTP 200. Only deployment-script failure diagnostics changed after the published app commit; no app source was changed for the retry. [Website evidence](production-web-verification.json).
- **Live browser:** seven routes each in desktop light and mobile dark: **14/14 pass**, zero page errors, error boundaries, failed fonts/scripts/styles or attempted API writes. Create shows its login gate, explicit sign-in stays on sign-in, Discover is public, and protected Settings redirects guests to Feed. The 74 recorded API reads include the expected anonymous managed-team 401 on each Discover visit; the other reads are 200. This is real production navigation, distinct from the local fixture-based 32-case rendering matrices. [Desktop results](browser-evidence/production-desktop.json), [mobile results](browser-evidence/production-mobile-dark.json).

The stricter local development matrix also completed 32 cases with no page errors and exactly four known settings-header attribute warnings, one per viewport/theme. Authenticated settings header dimensions settle correctly and Appearance works on desktop/mobile. The warning is documented above, not hidden. The final app typecheck, client suite and build-readiness gate were rerun after the last theme and empty-feed corrections.

To receive the OTA, open the installed runtime 1.0.5 app online, allow its background download, then close and reopen it. No new native binary or store submission was made. Follow the [tab-by-tab device test handoff](device-test-handoff.md). Publication makes these fixes available; it does not substitute for the pending native/provider acceptance.

[PR #281](https://github.com/xsantcastx/VarsityHubMobile/pull/281) remains OPEN; production is ahead of origin main. Merge the tested branch before treating main as the production baseline. In particular, an old-main API deploy must not bypass the ad-hold compatibility/rollback boundary above.

## Remaining acceptance gates

Native camera/gallery/crop/location/share, sandbox Apple/Google/Stripe purchase/recovery, actual email/push delivery, recurring cold-start sessions, representative workload performance and production backup recovery remain unverified. Direct Instagram Stories requires configuration and native integration/build. Minor-league schedule population requires an authorized provider; see [bounded plan](minor-league-ingest-plan.md). The deliberately privileged review account remains a product-policy decision, separate from tested organization-owner restrictions.
