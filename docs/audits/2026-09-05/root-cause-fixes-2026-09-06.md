# Matrix root-cause fixes — September 6, 2026

Local implementation on `fix/desktop-notes-map-collage-share`, based on `fccdc186d07d8f7588f0195e9655d0eb3ecb70a3`. These changes have not been committed, deployed to Railway, published as an OTA, or built into iOS. The [production audit](current-production-reaudit.md) retains the separate Railway, EAS and web version evidence; a latest Git commit does not prove production deployment.

## Findings and fixes

| Matrix finding      | Root cause and local change                                                                                                                                                                                                                                                                   | Acceptance evidence                                                                                                                                                                                                                  |
| ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ROLE-C01            | Bulk game creation bypassed the single-route organization approval gate. Both routes now use `gameCreation.ts` within their write transaction.                                                                                                                                                | Current team isolation suite: unapproved org rejected; approved control succeeds; no unauthorized writes.                                                                                                                            |
| ROLE-C02            | Game moderation state was written into the incompatible Event lifecycle enum. Shared conversion writes pending moderation as draft lifecycle. Game and linked Event creation are atomic; single and bulk pending caps share a serializable transaction. Each bulk row must name a team.       | Mixed own/foreign rows, fan single/bulk moderation, pending cap and malformed row rollback assertions.                                                                                                                               |
| ROLE-C03            | Team admin summary re-derived org ownership solely from membership. It now uses canonical `isOrgOwner`, including legacy owner pointers.                                                                                                                                                      | Legacy owner receives correct summary; foreign coaches, org managers and non-founders retain their existing restricted capabilities.                                                                                                 |
| AD-C01              | PaymentSheet stored the PaymentIntent in a session field, while refunds queried only the dedicated PI field. Initial logging and settlement now populate the dedicated field. Refund lookup supports the exact historical PI alias and repairs it atomically.                                 | Current ad persona suite: fan, coach and org-owner settlement/refund; fresh and historical PI records.                                                                                                                               |
| AD-C02              | Same-ad overlapping purchases collapsed into a single reservation, losing purchase ownership. The inventory adapter rejects already-paid dates and another active purchase's overlapping holds. Same-purchase replay and disjoint Run Again dates remain supported.                           | Paid overlap and checkout/PaymentSheet competing-hold rejection; refund preserves original paid delivery; historical hold compatibility tests.                                                                                       |
| SET-C01             | Server and browser locale date/time output disagreed during hydration. Initial render uses identical placeholders before local formatting. Removed the extra client DM after host-event creation: the server already owns notification, and DM failure could falsely report creation failure. | Fresh static web export and browser matrix 36/36; separate rendered success/failure submission tests.                                                                                                                                |
| Feedback loss       | Feedback returned success without storing the message. It now creates a durable existing AbuseReport record before returning a receipt. Retry keys are scoped to the authenticated reporter; retries neither change the original message nor send another notification.                       | Storage failure, notification failure, concurrent retry, reporter isolation and screenshot-host validation tests.                                                                                                                    |
| OBS-C01             | Telemetry identified native version but not the OTA. Sentry/PostHog now include update ID/channel; Sentry includes runtime and backend commit/deployment tags.                                                                                                                                | Observability tests; actual deployed ingestion remains pending.                                                                                                                                                                      |
| Monitoring failures | Uptime alert steps inherited success-only execution. Snyk execution failures appeared advisory; schedules scanned a stale default branch; raw scanner output reached remediation commits.                                                                                                     | Failure-aware health condition; structured Snyk clean/findings/unavailable classification; unavailable blocks; explicit scheduled production ref; sanitized SARIF and dependency-only PR paths. Wrapper tests and YAML parsing pass. |

The shared authorization model remains server-authoritative. Organization ownership is not founder administration. This patch fixes the reproduced inconsistencies; it is not a claim that every matrix scenario or physical-device flow has been certified.

## Historical ad data

At `2026-09-06T05:40:04.902Z`, the new read-only `server/scripts/audit-ad-purchase-provenance.ts` inspected the production primary database: **3 completed AD_PURCHASE records**, complete result, **0 missing PI references, 0 overlapping paid purchases, 0 unclassified records**. No records, provider payments or reservations were changed. This checks completed records only; it does not certify pending/failed/refunded purchases or reconcile provider receipts.

## Verification and retained failures

Raw local evidence is in `/tmp/varsityhub-root-fixes` and is not intended for publication; it can contain test fixtures. Production credentials were not written into the report.

- Fresh static export and browser settings matrix: **36/36 passed** (`browser-current.json`). That export tested the hydration fix before the subsequent removal of the duplicate client DM; rendered submission tests cover that later change.
- Focused final server rerun: **6 suites / 92 tests passed**, including current persona ads, refund reproduction, role tiers, cache invalidation, durable feedback and support authentication (`server-confirm-final.json`).
- Earlier focused game creation run: **5 suites / 33 tests passed** (`games-confirmed.json`).
- Earlier full client run: **218 suites / 1,541 tests passed** (`client-full.json`); a final full run including the new submission tests is recorded below.
- First full server run was not clean: 8 suites failed. Three tests encoded the superseded overlapping-hold behavior; they now distinguish rejected new overlaps from explicitly seeded historical holds. Four suites needed the existing `normalize_org_name_for_dedupe` SQL migration in the fresh local test database. Another collided on a shared founder fixture. All eight passed isolated reruns.
- The next concurrent full server run still had 4 failing suites / 5 assertions. One cache test mock omitted the newly transactional create methods; corrected. The three remaining suites passed sequentially. Do not treat these parallel failures as a clean full run; the final sequential run is recorded below.
- Snyk wrapper tests distinguish unavailable scanning from clean results and verify source snippets are removed. The actual local Snyk Code command returned **unavailable (exit 2)** because authentication was absent; no live SAST all-clear is claimed.

## Final verification record

- Full client: **219 suites / 1,543 tests passed** (`client-final.json`).
- Full server, sequential: **322 suites passed, 2 failed; 3,011 tests passed, 2 failed** (`server-serial-final.json`). Failures: `api-events` list-limit request timed out at 10 seconds; the founder email boundary test encountered an existing unique email fixture. This is not a clean full-suite result. A fresh isolated-database rerun is recorded below.
- Both client and server TypeScript checks exited 0. Expo lint, navigation audit, conflict-marker scan and secret scan passed. The worktree-aware error-envelope check caught a PaymentSheet error response; changed it to `sendError`, then reran successfully. `git diff --check` passed. Snyk wrapper: **2 tests passed**; three changed workflow YAML files parse successfully.

- Fresh isolated acceptance database: `api-events` **19/19** and organization email authority **15/15** passed (`acceptance-final.json`). The ad suite in that invocation correctly refused the database name because its dedicated-database safety prefix was not met; it did not execute those scenarios. Rerunning it against its designated local audit database passed **22/22** after the final error-envelope change (`ads-envelope-final.json`). No safety guard was weakened. All tests that failed in the full run have now passed focused reruns, but a single clean full server run has not been obtained.

## Release and unresolved acceptance

No schema change is required. Feedback uses the existing report table and reporter/target unique index. The temporary test-database migration was an existing repository migration, not a new production migration.

Server release and client publication are separate. Run the guarded release gates before shipping; client delivery uses `npm run update:production`, followed by deployed scenario checks. Record the deployed commit and OTA ID and verify that the new telemetry tags arrive. The scheduled Snyk workflow needs an actual production revision in `PRODUCTION_SOURCE_REF`, updated when that revision changes, plus working Snyk access. Workflow files must be installed on the repository's default branch for scheduled execution.

Rollback must preserve purchase references, existing paid inventory and historical holds. Reverting the overlap guard reintroduces the reproduced payment defect; prefer a forward fix. Existing report records remain readable if feedback code rolls back. No production migration, environment change, refund, outbound canary, push or publication occurred in this implementation.

Still open:

- **NATIVE-C01:** Sentry's `AIRMap insertReactSubview` crash is not fixed or reproduced on an installed device. Added attribution helps distinguish releases; it is not a native crash fix. Reproduce open/filter/reopen and background/foreground on the affected native release before selecting a maps/native patch. No paid EAS build was run.
- **OPS-C01:** dedicated private data-export storage is still unconfigured; code tests cannot activate it.
- **Provider acceptance:** PostHog authenticated ingestion/alerts, Snyk Code entitlement, Sentry/PostHog canary ingestion and actual alert destination receipt remain unverified. No external messages were sent.
- **Additional payment hardening:** `logTransaction` still tolerates persistence failure, and the adapter permits settlement without a matching log on some paths. This is a separate failure-injection follow-up, not resolved by adding the missing PI field. Do not claim all purchase-ledger failure modes are closed.

The operational loop is documented in [nightly production triage](../../operations/nightly-production-triage.md): detect, assign, reproduce, regression test, review, release, verify recovery, close. Automation can gather evidence and prepare patches; it must not label an unavailable monitor healthy or mark a fix live before release evidence exists.
